import numpy as np
import pandas as pd

IO = None

def gerar_curva_volumétrica() -> IO:
    escrever_na_planilha(url_da_planilha,
        simular_reservatório(
            parâmetros_de_entrada
        )
    )

def escrever_na_planilha(url_da_planilha: str, dados: pd.DataFrame) -> IO:
    pass

gerar_curva_volumétrica()